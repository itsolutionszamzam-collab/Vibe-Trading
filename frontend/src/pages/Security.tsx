import { useEffect } from "react";
const securityContact = "security@tradecorefx.com"; // Placeholder requiring confirmation before production use.
export function Security(){useEffect(()=>{document.title='Security | TradeCoreFX';},[]);return <main className="overflow-x-hidden px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl space-y-8"><section><p className="text-xs font-semibold uppercase tracking-[0.24em] text-primary">Security</p><h1 className="mt-4 max-w-4xl text-4xl font-bold tracking-tight">Security, Transparency and Responsible Design</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted-foreground">This page describes verified development-stage controls and known limitations. It does not imply certification, regulatory approval or guaranteed security.</p></section><Block title="Security Principles" items={["Minimise sensitive data exposure","Separate frontend and backend responsibilities","Avoid displaying secrets in browser code","Use transparent limitation disclosures"]}/><Block title="Current Development Controls" items={["API keys remain server-side where backend integrations require them","Local Ollama model support","API authentication key support","Private Codespaces development port support","Frontend/backend separation","Environment files excluded from Git","No live broker execution enabled during development"]}/><Block title="Data Handling" items={["No persistent journal storage in the current version","Remote API access can require authentication","Error responses should avoid exposing secrets","Sensitive values are not intentionally displayed in browser code"]}/><Block title="Local Model Architecture" items={["Local Ollama models can support workflow testing","Small local model usage is suitable for interface and integration testing, not production-grade market reasoning"]}/><Block id="known-limitations" title="Known Limitations" items={["Development-stage security posture","No formal third-party certification currently confirmed","Small local model used for workflow testing","Production hosting architecture not finalized","Authentication and subscription systems may still require production implementation"]}/><section className="rounded-3xl border bg-card p-6"><h2 className="text-2xl font-semibold">Responsible Disclosure</h2><p className="mt-3 text-muted-foreground">Security contact placeholder requiring confirmation before production: <span className="font-mono">{securityContact}</span>.</p></section><Block title="Security Roadmap" items={["Production authentication review","Subscription-access controls","Hosting architecture hardening","Operational monitoring and incident-response process"]}/></div></main>}
function Block({title,items,id}:{title:string;items:string[];id?:string}){return <section id={id} className="rounded-3xl border bg-card p-6"><h2 className="text-2xl font-semibold">{title}</h2><ul className="mt-4 grid gap-2 md:grid-cols-2">{items.map(i=><li key={i} className="rounded-xl border bg-background/60 p-3 text-sm text-muted-foreground">{i}</li>)}</ul></section>}
